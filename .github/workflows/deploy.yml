name: Build, Push Docker Image and Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up variables
        run: |
          echo "SHA_SHORT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
          echo "DATE=$(date +'%Y%m%d')" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/practica-continua:latest \
                       -t ${{ secrets.DOCKER_USERNAME }}/practica-continua:${{ env.SHA_SHORT }} \
                       -t ${{ secrets.DOCKER_USERNAME }}/practica-continua:${{ env.DATE }} .

      - name: Push Docker image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/practica-continua:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/practica-continua:${{ env.SHA_SHORT }}
          docker push ${{ secrets.DOCKER_USERNAME }}/practica-continua:${{ env.DATE }}

      - name: Deploy to Render
        run: |
          curl -X POST https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            -H "Content-Type: application/json"